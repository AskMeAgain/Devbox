{
    "variables": {
        "name": "dev",
        "disksize": "{{user `disksize`}}"
    },
    "builders": [
        {
            "vm_name": "{{user `vmname`}}",
            "type": "virtualbox-ovf",
            "keep_registered": true,
            "source_path": "./i3/output-updated/{{user `vmname`}}-temp2.ovf",
            "output_directory": "./i3/output-additions",
            "ssh_username": "dev",
            "ssh_password": "{{user `password`}}",
            "shutdown_command": "echo '{{user `password`}}' | sudo -S shutdown now",
            "boot_command": [
                "<wait24><leftSuperOn><enter><leftSuperOff><wait3>",
                "systemctl start sshd.service<enter><wait3>{{user `password`}}<enter>"
            ]
        }
    ],
    "provisioners": [
        {
            "type": "file",
            "source": "./scripts/manjaro-additions.sh",
            "destination": "~/manjaro-additions.sh"
        },
        {
            "type": "shell",
            "inline": "bash ~/manjaro-additions.sh {{user `password`}} {{user `dotfiles-git`}}"
        }
    ]
}